import { View, Text, SafeAreaView } from 'react-native';
import React from 'react';
import Form from './Screen/Setting/Form'
const App = () => {
  return (
    <SafeAreaView>
      <Form />
    </SafeAreaView>
  );
};

export default App;
